<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nginx Health Check</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            padding: 50px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            margin: 0;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .container {
            background: rgba(255, 255, 255, 0.1);
            padding: 40px;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        }
        h1 {
            font-size: 2.5rem;
            margin-bottom: 20px;
            color: #fff;
        }
        .status {
            font-size: 1.5rem;
            margin-bottom: 20px;
            color: #90EE90;
        }
        .info {
            font-size: 1rem;
            opacity: 0.8;
        }
        .timestamp {
            font-size: 0.9rem;
            opacity: 0.6;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🐧 Nginx Load Balancer</h1>
        <div class="status">✅ Healthy</div>
        <div class="info">
            <p>Red Hat Tools Demo - Load Balancer</p>
            <p>Port: {{ lb_port }}</p>
            <p>Backend: 127.0.0.1:{{ web_port }}</p>
        </div>
        <div class="timestamp">
            Last updated: <span id="timestamp"></span>
        </div>
    </div>

    <script>
        function updateTimestamp() {
            const now = new Date();
            document.getElementById('timestamp').textContent = now.toLocaleString();
        }
        
        updateTimestamp();
        setInterval(updateTimestamp, 1000);
    </script>
</body>
</html> 